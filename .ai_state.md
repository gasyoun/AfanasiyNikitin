# Project Objective: AfanasiyNikitin project

## ➡️ Next Steps (Queue)

- [ ] Fable 5 (`claude-fable-5`) — H486 Phase 5: author the narrative spine (`/put/`) keyed to the public-domain «Хожение» + the project's dataset (NOT a Khrustalev précis, see RIGHTS.md) and flesh out `/data/` docs. Also owns the §5 design decisions (final color/type choices) — this session's atlas.css rewrite kept values unchanged, only restructured into primitives→semantics.
- [ ] Decide open questions from H486 (narrative ship in release 1 vs later; EN locale = full translation or captions-only; `video_export` deliverable vs scratch) — park as `@DECIDE` if picked up before Fable's pass.
- [ ] Populate real per-visualization trust-block content (source artifact, n, date) in the 33 `atlas-docs/**/*.mdx` files — currently a generic placeholder citing Khrustalev (2026) + data/, not page-specific figures.
- [ ] Wire GitHub Pages deploy workflow (not added this session — H486 scoped scaffold+build-verify only, no `.github/workflows/deploy-pages.yml` yet).

## 🚧 Current Work-In-Progress (WIP)

- [ ] (no active task — H486 Phases 1–4, 6–7 complete this session)

## 🧠 Dev Notes & Hypotheses

- 10-07-2026 (Sonnet 5, `claude-sonnet-5`): H486 executed. Docusaurus 3.6.3 scaffolded with three docs-plugin instances (`put`, `atlas`, `data`) + a custom landing page — matches spec §4 IA. `atlas.css` restructured into primitives→semantic tokens (values unchanged; picking new colors/type is Fable's §5 job). The 7 inverted-ownership selectors (`.tm-f`, `.ms-ptab`, `.tv-card`, `.ms-cod`, `.brd-ev-row`, `.brd-leg`; `.bestiary-card` was dead/unused) removed from the shared stylesheet and their exact hover/active behavior pushed into the 4 owning pages' own `<style>` blocks — verified via browser preview that 3 of the 4 pages render identically standalone (afanasy_trade_marshruttnik, afanasy_manuscripts, three_travelers_comparison), afanasy_borders_animation only console/network-checked (no visual regression tooling issue this session).
- `atlas-theme.js` got a `storage`-event listener (H486 §2 defect 4) — confirmed live in the browser: toggling the Docusaurus navbar theme button propagates into an already-rendered same-origin iframe within the same tick (both parent and iframe read `data-theme="dark"` after the click), because Docusaurus's own color-mode plugin happens to use the same `localStorage['theme']` key.
- The 33 widgets moved to `static/atlas/` unchanged; `css/atlas.css` → `static/atlas/css/atlas.css`, `js/atlas-theme.js` → `static/atlas/js/atlas-theme.js`, `lib/` → `static/atlas/lib/`, `favicon.svg` → `static/atlas/favicon.svg` — all relative hrefs inside the 33 pages still resolve since they moved together.
- `afanasy_map_spine.html` fetches `data/places.csv` + `data/itinerary.csv` relative to its own location. `data/` stays at the repo root (H486 "never touch"); `scripts/sync-atlas-data.mjs` copies it into `static/atlas/data/` (gitignored, regenerated) at `prestart`/`prebuild` time so the fetch resolves without duplicating the data by hand.
- The pre-rebuild `index.html`'s `Dataset` JSON-LD block was salvaged verbatim into `docusaurus.config.mjs` `headTags` — except the LPF geojson `contentUrl`, which moved from `/data/places.lpf.geojson` to `/downloads/places.lpf.geojson` because `/data/` is now the Docusaurus IA route for the "Данные" docs instance, not a static-file mount (also synced by the same script, along with `datapackage.json`/`codemeta.json`/`.zenodo.json`/`CITATION.cff` → `static/`).
- Deleted per spec §4.2 (superseded by Docusaurus equivalents): `index.html`, `check_sw.html`, `sw.js`, `manifest.json`, `robots.txt`, `sitemap.xml`. Also deleted `test_pwa.js` (not explicitly named in H486, but it only tested those exact removed PWA files — kept as dead code would just error).
- `future.v4` in `docusaurus.config.mjs` is not a recognized field at pinned 3.6.3 (that's a 3.7+ config key) — removed it rather than upgrading Docusaurus (H486 pinned 3.6.3 per `/docusaurus-scaffold`'s webpack-5.97.1 compatibility note).
- Mobile-viewport (375px) scroll-containment could not be visually confirmed this session — `preview_resize` did not change `window.innerWidth` as read via `preview_eval` in this tool session (possible harness quirk), and `preview_screenshot` timed out repeatedly. The CSS itself (`AtlasFigure/styles.module.css` `.scrollArea{overflow-x:auto}` wrapping an iframe with `min-width:720px`, inside an unconstrained-width `.frameWrap`) is the standard containment pattern and measured correctly at the ~980px width the tool actually rendered (container width tracked the article width, not the iframe's min-width) — but a real mobile-viewport screenshot is still owed as a follow-up.
- `.github/workflows/deploy-pages.yml` was NOT added this session (out of the H486 phase list Sonnet owns — phases 1–4/6–7 don't mention Pages; `/docusaurus-scaffold`'s own Phase 2 does, so this is a gap between the two skill specs). GitHub Pages is also not yet enabled for gh-pages source on this repo.

## ✅ Completed (Recent only)

- 10-07-2026: H486 Phases 1, 2, 3, 4, 6, 7 (Sonnet 5, `claude-sonnet-5`) — Docusaurus scaffold, atlas.css two-tier token rewrite, theme storage-event propagation, 33 pages moved + `<AtlasFigure>` MDX wrappers + 6 theme galleries generated, i18n config (ru default/en second), build-verify (green, both locales, no gitignored file in bundle, JSON-LD present).
