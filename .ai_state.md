# Project Objective: AfanasiyNikitin project

## ➡️ Next Steps (Queue)

- [ ] Localize the browser-tab `<title>` site-name suffix for `en` (currently "Афанасий Никитин — Атлас" leaks onto every EN page after the translated page-title segment; Docusaurus doesn't localize `config.title` per-locale without extra plumbing) — cosmetic, not blocking.
- [ ] If EN narrative translation is ever wanted: `put-docs/` is still RU-only by MG's explicit ruling (10-07-2026) — only re-open if that ruling changes.

## 🚧 Current Work-In-Progress (WIP)

- [ ] (no active task — H498 complete: EN i18n for atlas+data + video_export tools split)

## ✅ H498 (10-07-2026, Sonnet 5 `claude-sonnet-5`)

MG's rulings on the three H486/H490 `@DECIDE` items:
1. **Narrative timing** — closed by superseding event, no action (H490 already shipped the full 7-chapter spine before the ruling landed).
2. **EN locale = captions + data docs only** — `i18n/en/` populated for all 32 themed atlas pages + 1 tools page (title/caption/trust-block), 7 sidebar category labels, `/atlas/` + `/data/` intros, both `/data/` sub-pages (model, provenance), navbar/footer/landing UI strings. `put-docs/` (narrative) deliberately untouched — stays RU-only. `scripts/generate-atlas-mdx.mjs` now emits RU+EN from one shared data table so the languages can't drift.
3. **`video_export` → own tools section** — moved from `atlas-docs/metod-i-istoriografiya/` to `atlas-docs/instrumenty/` ("Инструменты"/"Tools"), its own `_category_.json` positioned after the six analytical themes. Six themes now 32 pages combined, tools section holds the 33rd.

Build green both locales, 0 broken links, verified via `build/en/**` grep that translated
strings actually render (not RU fallback). CHANGELOG 1.7.0.

## 🧠 Dev Notes & Hypotheses

- 10-07-2026 (Sonnet 5, `claude-sonnet-5`, H498): `write-translations --locale en` was used to scaffold the UI-chrome JSON skeletons (navbar/footer/code.json/sidebar-category current.json) rather than hand-guessing Docusaurus's translation-key naming convention — safer and matches what the framework will actually look up.
- Internal `/atlas/` links (in both `atlas-docs/intro.md` and its EN counterpart) were switched from root-relative (`/atlas/theme/slug`) to file-relative-with-extension (`./theme/slug.mdx`) — the former resolves relative to the site root regardless of active locale (breaking under `/en/`), the latter resolves relative to the source file and is locale-correct. This generalizes the fix H486/H490 already applied ad hoc to a couple of links; worth remembering for any future hand-authored cross-link in `atlas-docs/` or `data-docs/`.
- `docusaurus.config.mjs`'s global `title`/`tagline` are evaluated once and are NOT automatically per-locale — each locale's full production build (`[INFO] [en] Creating an optimized production build...`) reuses the same static config object. Per-page titles ARE correctly localized (they come from each MDX file's own frontmatter/`<Translate>` calls); only the trailing site-name suffix Docusaurus appends to `<title>` stays Russian on EN pages. Not fixed this pass (cosmetic, out of H498's explicit scope) — flagged as a residual in Next Steps.
- Mint-collision note: `mint_handoff.py`'s claim-push for H498 reported "could not push... only locally claimed" but the content landed on `origin/main` anyway (confirmed via a later `git ls-tree origin/main`) — the warning appears to be conservative/racy under heavy concurrent-session load on Uprava that day; don't assume a WARN means the push definitely failed, verify against origin before re-minting.
- (H486/H490 dev notes from prior sessions trimmed — see git history of this file for the full record if needed; the ones still load-bearing were folded into README.md/CHANGELOG.md at the time.)

## ✅ Completed (Recent only)

- 10-07-2026: H498 (Sonnet 5, `claude-sonnet-5`) — EN i18n (atlas captions + data docs, not narrative), video_export moved to its own tools section, CHANGELOG 1.7.0.
- 10-07-2026: H490 (Fable 5, `claude-fable-5`) — `/put/` 7-chapter narrative spine, `/data/` model+provenance docs, §5 design system finalized, real per-visualization trust blocks. CHANGELOG 1.6.0.
- 10-07-2026: H486 Phases 1, 2, 3, 4, 6, 7 (Sonnet 5, `claude-sonnet-5`) — Docusaurus scaffold, atlas.css two-tier token rewrite, theme storage-event propagation, 33 pages moved + `<AtlasFigure>` MDX wrappers + 6 theme galleries generated, i18n config (ru default/en second), GitHub Pages switched to Actions deploy, build-verify (green, both locales, no gitignored file in bundle, JSON-LD present).

## 🧠 Dev Notes — merged from the legacy `AI_STATE.md` (last updated 14-06-2026, Opus 4.8 `claude-opus-4-8`; file retired by H704, 12-07-2026)

Durable items that were only recorded there (pre-Docusaurus-rebuild provenance — re-verify paths against the H486+ site where they name files):

- **Release flow:** auto-merge is DISABLED on this repo; land via PR → `gh pr checks <n> --watch` → merge → `gh release create vX.Y.Z --target main`. Direct push to `main` is blocked.
- **Versioning split:** site (CHANGELOG + tags) versions independently from the dataset (datapackage/CITATION/.zenodo/schema.org); dataset bumps ONLY on `data/` changes — 1.1.0 → 1.2.0 is still owed at the next real `data/` change.
- **`source_page` citations** (the book-companion payoff) are blocked on the in-copyright Khrustalev 2026 book (gitignored) — a human enters page refs; agent fills + validates + bumps the dataset version. `data/QUALITY.md` measured `source_page` 0% on places/itinerary/people/editions/citations.
- **Human gates parked there:** Zenodo DOI wiring (now ALSO org-frozen until 15-07-2026) · WHG upload (`SUBMISSION_WHG.md`) · real-device PWA install test.
- **CI widget-drift gate:** editing a `data/*.csv` row requires regenerating the affected widget's `*_BUNDLED` or `tools/validate_widgets.cjs` fails CI (`data-validate` workflow) — still true for the iframed viz.
