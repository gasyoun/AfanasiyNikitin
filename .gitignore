http-server.*.log
*.log
scratch/
hrustalev_tetradi_2026.pdf

# audit sidecars
.contributing_old.md
.contributing_audit.txt

# in-copyright-derived — see RIGHTS.md (kept local, not published)
tetradi_hrustalev_2026.md
article_drevnyaya_rus_draft.md

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Docusaurus
/node_modules
/build
/.docusaurus
/.cache-loader
~$*

# generated at build/start time by scripts/sync-atlas-data.mjs — data/ (repo root) is the source of truth
/static/atlas/data/
/static/downloads/
/static/datapackage.json
/static/codemeta.json
/static/.zenodo.json
/static/CITATION.cff

# H4631 sandbox/credential baseline (13-09-2026): real env files are never
# committed — house convention keeps secrets in ~/.secrets/;
# .env.example stays tracked.
.env
.env.*
!.env.example
